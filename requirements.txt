
# --- 웹 서빙 필수 라이브러리 ---
flask==3.1.3
pymongo==4.17.0
numpy
pandas
Pillow
scikit-learn

# 배포 용량 절감을 위해 headless 버전 권장
opencv-python-headless

# --- 딥러닝 모델 추론용 ---
# 만약 서버에서 PyTorch 모델(.pt)을 불러와 추론해야 한다면 CPU 전용 torch 필요
# (CUDA/GPU 라이브러리가 빠져서 용량이 1/10로 줄어듭니다)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
grad-cam==1.5.5
